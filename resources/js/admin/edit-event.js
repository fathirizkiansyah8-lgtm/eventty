/**
 * Edit Event JS
 * Form submission ditangani native POST @method('PUT') di blade.
 * File ini hanya mengatur banner preview.
 */
document.addEventListener('DOMContentLoaded', function () {
    // Banner preview sudah ada sebagai inline script di blade.
    // File ini tidak perlu melakukan apa-apa lagi.
    // (create-event.js masih menggunakan AJAX untuk POST)
});
